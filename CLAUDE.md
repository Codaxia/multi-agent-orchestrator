# Dashboard Agents — AI Assistant Entry Point

This file is auto-loaded by Claude Code at session start.

**Read `AGENTS.md` for the full instructions** — it contains the complete work protocol,
the dashboard API reference, and the agent definitions index.

> `AGENTS.md` is LLM-agnostic. The same instructions apply regardless of which
> AI assistant is running the pipeline.
